extends layout


block content
  h1= title
  p اهلا بك في #{title}
    for country in countries
      p #{country.id} - #{country.country_name} - #{country.nationality_male} - #{country.nationality_female} 

